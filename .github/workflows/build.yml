name: Build
on:
  workflow_dispatch:
  push:
    branches:
    - 'master'
    paths-ignore:
    - '**.md'
    - '**.gitignore'
    - '**.opam'
    - '**.nix'
    - 'LICENSE'
  pull_request:
    paths-ignore:
    - '**.md'
    - '**.gitignore'
    - '**.opam'
    - '**.nix'
    - 'LICENSE'
concurrency:
  group: "${{ github.workflow }}-${{ github.event.pull_request.number || github.head_ref || github.ref }}"
  cancel-in-progress: true
permissions:
  contents: read
env:
  OCAML_COMILER_VERSION: "4.14.2"
  JOBS: 4
jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout branch ${{ github.ref_name }}
        uses: actions/checkout@v6

      - name: Set up OCaml
        uses: ocaml/setup-ocaml@v3
        with:
          ocaml-compiler: ${{env.OCAML_COMILER_VERSION}}
          opam-repositories: |
            rocq-released: https://rocq-prover.org/opam/released
            default: https://opam.ocaml.org
          opam-pin: false

      - run: sudo apt-get update
      - name: Restore opam cache
        id: opam-cache
        uses: actions/cache@v4
        with:
          path: ${{github.workspace}}/_opam
          key: opam-${{env.OCAML_COMILER_VERSION}}-${{hashFiles('.github/deps.opam.locked')}}
          restore-keys: |
            opam-${{env.OCAML_COMILER_VERSION}}-

      - name: Build dependencies
        run: |
          opam install --deps-only -j${{ env.JOBS }} .github/deps.opam.locked
          opam clean -a -c -s --logs

      - name: Set up environment
        run: |
          echo "::group::Setting up problem matcher"
          echo "::add-matcher::./.github/rocq-errors.json"
          echo "::endgroup::"

      - name: Build Core
        run: |
          echo "::group::Configure build"
          opam exec -- ./configure.sh local
          echo "::endgroup::"

          echo "::group::Build Common"
          opam exec -- make -j${{ env.JOBS }} common
          echo "::endgroup::"

          echo "::group::Build Elm"
          opam exec -- make -j${{ env.JOBS }} elm
          echo "::endgroup::"

          echo "::group::Build Rust"
          opam exec -- make -j${{ env.JOBS }} rust
          echo "::endgroup::"

          echo "::group::Build Plugin"
          opam exec -- make -j${{ env.JOBS }} plugin
          echo "::endgroup::"

      - name: Build Tests
        run: |
          echo "::group::Build Tests"
          opam exec -- make -j${{ env.JOBS }} tests
          echo "::endgroup::"

      - name: Upload extraction results
        uses: actions/upload-artifact@v5
        with:
          name: extraction-results
          path: tests/extracted-code
          retention-days: 2

      - name: Build documentation
        if: github.event_name == 'push' && github.ref == 'refs/heads/master'
        run: |
          echo "::group::Running rocqdoc"
          opam exec -- make -j${{ env.JOBS }} html
          echo "::endgroup::"

      - name: Prepare documentation for deployment
        if: github.event_name == 'push' && github.ref == 'refs/heads/master'
        uses: actions/upload-pages-artifact@v4
        with:
          path: docs

  test-rust:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout branch ${{ github.ref_name }}
        uses: actions/checkout@v6

      - name: Download extraction results
        uses: actions/download-artifact@v7
        with:
          name: extraction-results
          path: tests/extracted-code

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@v1
        with:
          toolchain: stable

      - name: Test Rust extraction
        run: |
          echo "::group::Running Rust Tests"
          make -j${{ env.JOBS }} -C tests test-rust-extraction
          echo "::endgroup::"

  test-elm:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout branch ${{ github.ref_name }}
        uses: actions/checkout@v6

      - name: Download extraction results
        uses: actions/download-artifact@v7
        with:
          name: extraction-results
          path: tests/extracted-code

      - name: Set up Elm
        run: |
          curl -L -o /tmp/elm.gz https://github.com/elm/compiler/releases/download/0.19.1/binary-for-linux-64-bit.gz
          gunzip /tmp/elm.gz
          sudo chmod +x /tmp/elm
          sudo mv /tmp/elm /usr/local/bin/
      - name: Set up elm-test
        uses: actions/setup-node@v6
        with:
          node-version: 16
      - name: Set up elm-test
        run: |
          npm install -g elm-test@0.19.1-revision9

      - name: Test Elm extraction
        run: |
          echo "::group::Running Elm Tests"
          make -j${{ env.JOBS }} -C tests test-elm-extraction
          echo "::endgroup::"

  deploy-docs:
    if: github.event_name == 'push' && github.ref == 'refs/heads/master'
    needs: build
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
